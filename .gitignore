# Ignore all .env files everywhere
**/*.env

# OS junk
.DS_Store

# Python cache / compiled
__pycache__/
*.py[cod]

# SQLite DBs and logs
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.log

# Any directory named or starting with "runs" (runs, runs1, runs-prod)
**/runs*/

# Any directory named or starting with "run" (run, run1, run2, run_batch_*)
**/run*/

# Any directory named or starting with "old" (old, old_backups, old_files)
**/old*/

# Your termination / experiment dirs (note typo: Termintation vs Termination)
**/Termintation*/
**/Termination*/

# Any directory named or starting with "test" (test, tests, test_data, etc.)
**/test*/

# (Optional) keep .gitkeep files in ignored directories if needed
!**/runs*/.gitkeep
!**/old*/.gitkeep
